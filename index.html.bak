<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Proxmox VNC 뷰어 (React)</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 20px; background: #f5f5f5; }
    .container { max-width: 1200px; margin: 0 auto; background: #fff; border-radius: 8px; box-shadow: 0 2px 10px rgba(0,0,0,.1); overflow: hidden; }
    .header { background: #2c3e50; color: #fff; padding: 20px; }
    .vm-selector { padding: 20px; border-bottom: 1px solid #eee; }
    select, button { padding: 10px; margin: 5px; border: 1px solid #ddd; border-radius: 4px; font-size: 14px; }
    button { background: #3498db; color: #fff; border: none; cursor: pointer; transition: background .3s; }
    button:hover { background: #2980b9; }
    button:disabled { background: #95a5a6; cursor: not-allowed; }
    .vnc-container { position: relative; background: #000; height: 80vh; width: 100%; overflow: hidden; }
    .status { padding: 10px; margin: 10px; border-radius: 4px; font-weight: bold; }
    .status.connecting { background: #f39c12; color: #fff; }
    .status.connected { background: #27ae60; color: #fff; }
    .status.error { background: #e74c3c; color: #fff; }
    .controls { padding: 10px 20px; background: #ecf0f1; display: flex; gap: 10px; align-items: center; }
    .vm-info { margin: 0 10px; font-size: 14px; color: #666; }
  </style>
</head>
<body>
  <div id="root"></div>

  <!-- React via CDN -->
  <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
  <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>

  <!-- noVNC RFB를 전역으로 노출 (Express가 noVNC를 정적으로 서빙한다고 가정) -->
  <script type="module">
    import RFB from '/core/rfb.js';
    window.RFB = RFB;
  </script>

  <!-- React 앱 코드 분리 (served from noVNC/app.js) -->
  <script src="/app.js"></script>
</body>
</html>

